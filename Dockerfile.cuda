# Dockerfile.cuda ─────────────────────────────────────────────────────
# Author : ChatGPT for CBW  ✦ 2025-05-23
# Summary: CUDA-enabled container for NVIDIA GPU acceleration
# Usage  : docker build -f Dockerfile.cuda -t jam-splitter-agent:cuda .
# ModLog : 2025-05-23  Initial version

FROM nvidia/cuda:11.8-runtime-ubuntu22.04

# Create non-root user
RUN useradd --create-home agentuser
WORKDIR /home/agentuser/app

# System deps
RUN apt-get update && apt-get install -y python3-pip && rm -rf /var/lib/apt/lists/*

# Copy and install Python deps
COPY requirements.txt ./
RUN pip3 install --no-cache-dir -r requirements.txt

# Copy application code
COPY . .
RUN chown -R agentuser:agentuser .
USER agentuser

ENTRYPOINT ["python3", "main.py"]
